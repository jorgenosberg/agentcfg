# Contributing

Thanks for helping me improve and maintain `agentcfg`.

Before opening a change, run:

```sh
make check
```

Use conventional commit messages, for example `fix(sync): detect nested copy drift`. Keep pull request descriptions short and specific.
